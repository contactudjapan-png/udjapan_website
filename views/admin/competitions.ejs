<%- include('../partials/head') %>
<body class="admin-page">
<%- include('../partials/nav-admin') %>
<%- include('../partials/flash') %>

<main class="admin-container">
  <%- include('../partials/event-subnav') %>
  <div class="section-header">
    <h1><%- t('admin.pg_competitions') %> — <%= event.title %></h1>
  </div>

  <form action="/admin/events/<%= event.id %>/competitions/new" method="POST" class="admin-form" style="max-width:600px;margin-bottom:2rem">
    <h2 style="font-size:1rem;margin-bottom:1rem"><%- t('admin.add_competition') %></h2>
    <div class="form-row">
      <div class="form-group">
        <label for="name"><%- t('admin.lbl_comp_name') %></label>
        <input type="text" id="name" name="name" required placeholder="<%- t('admin.notes_ph') %>">
      </div>
      <div class="form-group">
        <label for="winner_name"><%- t('admin.lbl_winner_name') %></label>
        <input type="text" id="winner_name" name="winner_name" placeholder="<%- t('admin.winner_ph') %>">
      </div>
    </div>
    <div class="form-group">
      <label for="notes"><%- t('admin.lbl_notes') %></label>
      <input type="text" id="notes" name="notes" placeholder="<%- t('admin.notes_ph') %>">
    </div>
    <button type="submit" class="btn btn-primary"><%- t('btn.add_competition') %></button>
  </form>

  <% if (competitions.length === 0) { %>
    <div class="empty-state"><p><%- t('admin.empty_competitions') %></p></div>
  <% } else { %>
    <table class="admin-table">
      <thead><tr>
        <th><%- t('tbl.comp_name') %></th><th><%- t('tbl.winner') %></th><th><%- t('tbl.comment') %></th><th><%- t('tbl.actions') %></th>
      </tr></thead>
      <tbody>
      <% competitions.forEach(comp => { %>
        <tr>
          <td><strong><%= comp.name %></strong></td>
          <td>
            <% if (comp.winner_name) { %>
              <span style="color:var(--green);font-weight:600">🏆 <%= comp.winner_name %></span>
            <% } else { %>
              <span style="color:var(--text-muted)">—</span>
            <% } %>
          </td>
          <td><%= comp.notes || '—' %></td>
          <td class="action-cell">
            <button type="button" class="btn btn-sm btn-outline edit-comp-btn"
              data-id="<%= comp.id %>"
              data-name="<%= comp.name %>"
              data-winner="<%= comp.winner_name || '' %>"
              data-notes="<%= comp.notes || '' %>">
              <%- t('btn.edit') %>
            </button>
            <form action="/admin/competitions/<%= comp.id %>/delete" method="POST" style="display:inline" onsubmit="return confirm('মুছে ফেলবেন?')">
              <button type="submit" class="btn btn-sm btn-danger"><%- t('btn.delete') %></button>
            </form>
          </td>
        </tr>
      <% }) %>
      </tbody>
    </table>
  <% } %>
</main>

<!-- Edit modal -->
<div id="edit-modal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,0.5);z-index:500;align-items:center;justify-content:center">
  <div class="card" style="width:100%;max-width:480px;padding:1.5rem">
    <h2 style="margin:0 0 1rem;font-size:1rem"><%- t('admin.edit_competition') %></h2>
    <form id="edit-form" method="POST" class="admin-form">
      <div class="form-group">
        <label><%- t('admin.lbl_comp_name') %></label>
        <input type="text" id="edit-name" name="name" required>
      </div>
      <div class="form-group">
        <label><%- t('admin.lbl_winner_name') %></label>
        <input type="text" id="edit-winner" name="winner_name" placeholder="<%- t('admin.winner_ph') %>">
      </div>
      <div class="form-group">
        <label><%- t('admin.lbl_notes') %></label>
        <input type="text" id="edit-notes" name="notes">
      </div>
      <div style="display:flex;gap:0.5rem">
        <button type="submit" class="btn btn-primary"><%- t('admin.btn_save') %></button>
        <button type="button" id="close-modal" class="btn btn-outline"><%- t('btn.cancel') %></button>
      </div>
    </form>
  </div>
</div>

<script>
(function () {
  var modal = document.getElementById('edit-modal');
  document.addEventListener('click', function (e) {
    var btn = e.target.closest('.edit-comp-btn');
    if (btn) {
      document.getElementById('edit-form').action = '/admin/competitions/' + btn.dataset.id + '/edit';
      document.getElementById('edit-name').value = btn.dataset.name;
      document.getElementById('edit-winner').value = btn.dataset.winner;
      document.getElementById('edit-notes').value = btn.dataset.notes;
      modal.style.display = 'flex';
    }
  });
  document.getElementById('close-modal').addEventListener('click', function () {
    modal.style.display = 'none';
  });
})();
</script>

<%- include('../partials/footer') %>
</body>
</html>
